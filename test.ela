#написать_в_файл("../hello.txt", "Hello world");
пауза();
новая_переменная(число1, 0);
новая_переменная(число2, 0);

цикл(0, 100, 1, {
	цикл(0, 100, 1, {
		умножение(рез, число1, число2);
		вывод(число2, " * ", число1, " = ", рез);	
        сумма(число1, число1, 1);
	});
	сумма(число2, число2, 1);
    новая_переменная(число1, 0);
});